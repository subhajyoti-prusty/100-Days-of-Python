# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)
# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list

large_dict = {i: [0] * m for i in range(n)}

# To verify the size
print(len(large_dict))  # Should print 1000000
print(len(large_dict[0]))  # Should print 1000

n = 10**6  # Number of tuples
m = 10**3  # Number of elements in each tuple

large_set_of_tuples = {(i,)*m for i in range(n)}

# To verify the size
print(len(large_set_of_tuples))  # Should print 1000000

import numpy as np

x = 100  # Dimension 1
y = 100  # Dimension 2
z = 100  # Dimension 3

large_3d_array = np.zeros((x, y, z))

# To verify the size
print(large_3d_array.shape)  # Should print (100, 100, 100)

import pandas as pd

n = 10**6  # Number of rows
m = 10**2  # Number of columns

large_dataframe = pd.DataFrame([[0]*m for _ in range(n)])

# To verify the size
print(large_dataframe.shape)  # Should print (1000000, 100)

# Generate python codes with hight space complexity

n = 10**6  # Number of inner lists
m = 10**3  # Number of elements in each inner list

large_list_of_lists = [[0] * m for _ in range(n)]

# To verify the size
print(len(large_list_of_lists))  # Should print 1000000
print(len(large_list_of_lists[0]))  # Should print 1000

n = 10**6  # Number of keys
m = 10**3  # Number of elements in each list
