# Here we are storeing the value the usear have entered in a variable named "name". So that we can easily access it 
name = input("What is your name?")
print(name)

#Here wea are first storing the usear input data in a variable "name" the storeing the length of the variable
#in a varibale called "length". So that ir can easily accessable 
name = input("What is your name?")
length =len(name)
print(length)